//Oefening 1
function calculate(getal1, getal2) {
    getal1 = Number(prompt('Type een getal'));
    getal2 = Number(prompt('Type een getal'));

    let antwoord = getal1 + getal2;
    alert(antwoord);
}

calculate()//Roept de functie op.

//Oefening 2
function calculate(getal1, getal2) {
    getal1 = Number(prompt('Type een getal'));
    getal2 = Number(prompt('Type een getal'));

    let antwoord = getal1 + getal2;
    alert(antwoord);
}